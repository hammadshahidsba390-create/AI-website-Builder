{
  "name": "ai-website-builder-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=server && npm run build --workspace=client"
  }
}
